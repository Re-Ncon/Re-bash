# Re-bash
Скрипт `Re-bash` представляет собой комплексную утилиту (Bash-скрипт) для автоматизации администрирования, настройки и развертывания программного обеспечения на серверах и рабочих станциях (АРМ) под управлением ОС Astra Linux 1.6.*-1.7.*

![OS](https://img.shields.io/badge/OS-Astra_Linux-blue?style=flat-square)
![Bash](https://img.shields.io/badge/Language-Bash-green?style=flat-square)
![License](https://img.shields.io/badge/License-MIT-yellow?style=flat-square)

## 📥 Установка и запуск

Вам не обязательно скачивать весь репозиторий, достаточно загрузить сам исполняемый файл скрипта.

### 1. Скачивание скрипта
Загрузите скрипт на рабочую станцию с помощью утилиты `wget` (файл сохранится как `Re_bash.sh`):
```bash
wget -O Re_bash.sh [https://raw.githubusercontent.com/Re-Ncon/Re-bash/main/Re%20bash.sh](https://raw.githubusercontent.com/Re-Ncon/Re-bash/main/Re%20bash.sh)
chmod +x Re_bash.sh
sudo ./Re_bash.sh
```
[📥 Скачать актуальную версию (Релизы)](https://github.com/Re-Ncon/Re-bash/releases/)

## ИНФОРМАЦИЯ
### 📊 Сводная таблица модулей

| Категория | Технологии | Поддерживаемый функционал |
| :--- | :--- | :--- |
| 🛡 **Антивирусы** | **Kaspersky, Dr.Web** | Развертывание сервера KSC (WEB + MariaDB), сетевая установка KESL и KLNAGENT на АРМ, исправление прав. Применение патчей Dr.Web для секретных сессий. |
| 🔐 **Криптография** | **Jacarta** | Полный цикл работы с токенами: установка клиентского ПО, сервера аутентификации и модулей администратора. Удаление пакетов. |
| 🔑 **Удаленный доступ** | **SSH** | Установка пакетов, генерация (RSA/ed25519) и автоматическая рассылка ключей, настройка беспарольного доступа, управление доступом `root`. |
| 🏢 **Домены** | **ALD, FreeIPA** | Развертывание контроллеров домена (Сервер/Резерв), автоматизированный ввод и вывод рабочих станций (Клиент) из домена. |
| 📂 **Файловые помойки**| **SMB, NFS** | Быстрая настройка сетевых файловых систем для обмена данными между Windows- и Linux-машинами. |
| 🌍 **Сетевые службы** | **DNS, NTP** | Настройка локального сервера доменных имен и службы точного времени для синхронизации всех узлов сети. |
| 💿 **Сетевая загрузка** | **PXE** | Развертывание сервера (DHCP + TFTP) для массовой удаленной установки ОС Astra Linux на новые машины. |
| 📈 **Софт и Мониторинг**| **Zabbix, Thunderbird** | Установка агентов Zabbix для мониторинга инфраструктуры и автоматическая базовая настройка почтового клиента Thunderbird. |

---
## 📋 Важные инструкции и особенности работы

Перед началом работы со скриптом **RENCON** обязательно ознакомьтесь со следующими правилами и механиками его работы:

### 👑 1. Запуск с правами суперпользователя (root)
Скрипт выполняет глубокую настройку ОС: изменяет системные конфигурационные файлы (в `/etc/`), управляет службами (`systemctl`), устанавливает пакеты и настраивает сеть.
*   **Требование:** Скрипт должен выполняться **строго с правами root**.
*   **Как запускать:** Используйте команду `sudo ./rencon.sh` или предварительно перейдите в сеанс суперпользователя с помощью `sudo su` / `su -`. При запуске от обычного пользователя скрипт не сможет применить настройки.

### 📦 2. Для чего нужны `.deb` файлы?
Скрипт **автоматизирует** процесс установки, настройки и исправления ошибок, но он **не скачивает** проприетарное программное обеспечение (такое как Kaspersky, Dr.Web или Jacarta) из интернета. 
*   **Причина:** Это сделано специально для возможности работы в изолированных корпоративных сетях (без выхода в интернет), а также из-за лицензионных ограничений самих продуктов.
*   **Что делать администратору:** Для работы модулей установки ПО вы должны заранее скачать нужные `.deb` пакеты (например, `kesl_*.deb`, `klnagent_*.deb` или пакеты Jacarta) и положить их в директорию рядом со скриптом. Утилита сама найдет их и запустит тихую установку.

### 📝 3. Система логирования (Журналы работы)
Вам не придется гадать, успешно ли отработал скрипт или на каком ПК возникла проблема — утилита ведет подробный учет действий.
*   **Где сохраняются:** Лог-файлы генерируются автоматически и сохраняются прямо в рабочей директории скрипта.
*   **Что внутри логов:**
    *   **Сетевая диагностика:** При использовании массового пинга (модуль 7) создаются файлы вида `ping_log_*.log`, где зафиксированы все ответившие и недоступные IP-адреса.
    *   **Установка ПО:** При установке пакетов (например, Jacarta) вывод терминала перенаправляется в инсталляционные логи (например, `jacarta_install_*.log`). Это позволяет легко отследить ошибки зависимостей или нехватку места на диске.
*   **Очистка:** Вы можете безболезненно удалять эти `.log` файлы после завершения работы — при необходимости скрипт сгенерирует новые.

```
RENCON v-*.*.*
├── [1] Управление SSH и рассылка файлов
│   ├── Установка / Удаление SSH-сервера
│   ├── Генерация SSH-ключей (RSA/ed25519)
│   ├── Рассылка ключей для беспарольного доступа
│   ├── Удаление старых SSH-ключей
│   ├── Настройка доступа root (Блокировка / Разблокировка)
│   └── Массовая рассылка файлов/директорий по диапазону IP
│
├── [2] Антивирусная защита
│   ├── Kaspersky
│   │   ├── Развертывание KSC (Web-консоль + MariaDB)
│   │   ├── Сетевая установка KESL и агента (KLNAGENT)
│   │   └── Локальная настройка (Смена IP KSC, права, источник обновлений)
│   └── Dr.Web
│       └── Применение патча для работы в секретной сессии Astra Linux
│
├── [3] Работа с токенами Jacarta
│   ├── Установка серверного модуля
│   ├── Установка клиентского ПО
│   ├── Установка пакета администратора
│   └── Полное удаление ПО Jacarta
│
├── [4] Доменная инфраструктура
│   ├── ALD (Astra Linux Directory)
│   │   ├── Развертывание (Сервер / Резервный сервер)
│   │   └── Ввод АРМ (Клиент)
│   ├── FreeIPA
│   │   ├── Развертывание Сервера
│   │   └── Настройка Клиента
│   └── Удаление / Вывод АРМ из домена
│
├── [5] Сетевые и системные службы
│   ├── SMB (Samba) — настройка общих ресурсов (Windows/Linux)
│   ├── NFS — сетевая файловая система (UNIX)
│   ├── DNS — развертывание локального сервера имён
│   ├── NTP — настройка сервера и синхронизация точного времени
│   └── PXE — установка ОС по сети (DHCP + TFTP)
│
├── [6] ПО и Мониторинг
│   ├── Zabbish — развертывание агента / сервера
│   └── Thunderbird — автоматическая конфигурация почтового клиента
│
└── [7] Диагностика и Логирование
    ├── Проверка доступности (Ping)
    │   ├── Парсинг адресов из /etc/hosts
    │   └── Сканирование кастомных диапазонов IP
    ├── Форматированный табличный вывод статусов
    └── Автоматическое сохранение результатов в .log файлы```
```
### 5. 💰 Поддержать разработчика

BTC `1CR8ttUftrfWCZPropQtDsLiDYxQsaMdoQ`

ETH ERC20 `0x5991ad6b9359740ab16df67cdafe44d05030d128`
